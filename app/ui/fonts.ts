import { Inter } from 'next/font/google';
export const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

import { Lusitana } from 'next/font/google';
export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: ['400', '700'],
  variable: '--font-lusitana',
  display: 'swap',
});

import { Rubik_Glitch } from 'next/font/google';
export const rubikGlitch = Rubik_Glitch({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-rubik-glitch',
  display: 'swap',
});

import { Limelight } from 'next/font/google';
export const limelight = Limelight({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-limelight',
  display: 'swap',
});

import { Monsieur_La_Doulaise } from 'next/font/google';
export const french = Monsieur_La_Doulaise({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-french',
  display: 'swap',
});

import { UnifrakturMaguntia } from 'next/font/google';
export const german = UnifrakturMaguntia({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-german',
  display: 'swap',
});

import { Nabla } from 'next/font/google';
export const nabla = Nabla({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-nabla',
  display: 'swap',
});
